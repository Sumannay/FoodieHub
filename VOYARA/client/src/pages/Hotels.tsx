import { ArrowRight, BedDouble, MapPin, Search, Star, Users } from "lucide-react";
import { useMemo, useState } from "react";
import { Link } from "react-router-dom";
import { formatINR, stays } from "../data/catalog";

export default function Hotels() {
  const [query, setQuery] = useState("");
  const [type, setType] = useState("All");
  const [guests, setGuests] = useState(2);
  const types = ["All", ...new Set(stays.map((stay) => stay.type))];
  const visible = useMemo(() => {
    const term = query.trim().toLowerCase();
    return stays.filter((stay) => (type === "All" || stay.type === type) && (!term || [stay.name, stay.destination, stay.state, stay.type].join(" ").toLowerCase().includes(term)));
  }, [query, type]);
  return <main className="min-h-screen bg-[#fbfaf7] pb-16"><section className="bg-[#183927] px-5 py-16 text-white sm:px-8 lg:px-10"><div className="mx-auto max-w-7xl"><p className="text-xs font-bold uppercase tracking-[.2em] text-[#d8fbcc]">Stays</p><h1 className="mt-4 text-4xl font-black tracking-[-.05em] sm:text-6xl">Rest well, then go further.</h1><p className="mt-5 max-w-2xl leading-7 text-white/75">Browse sample stays in the current catalogue. Every stay now checks adults, children and rooms before a booking can continue.</p><div className="mt-8 grid max-w-4xl gap-3 rounded-2xl bg-white p-3 text-slate-900 md:grid-cols-[1fr_auto]"><label className="flex items-center gap-3 px-2"><Search size={19} className="text-slate-400" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search Puri, Konark, Bhubaneswar or hotel…" className="min-w-0 flex-1 bg-transparent py-2 text-sm outline-none" /></label><div className="flex items-center gap-3 rounded-xl bg-slate-50 px-3 py-2 text-sm"><Users size={17} className="text-slate-400" /><button type="button" onClick={() => setGuests((value) => Math.max(1, value - 1))} className="grid size-7 place-items-center rounded-full bg-white font-black">−</button><b>{guests} guests</b><button type="button" onClick={() => setGuests((value) => value + 1)} className="grid size-7 place-items-center rounded-full bg-[#183927] text-white">+</button></div></div></div></section><section className="mx-auto max-w-7xl px-5 py-10 sm:px-8 lg:px-10"><div className="flex gap-2 overflow-x-auto pb-2">{types.map((item) => <button key={item} type="button" onClick={() => setType(item)} className={"shrink-0 rounded-full px-4 py-2.5 text-sm font-bold " + (type === item ? "bg-[#183927] text-white" : "bg-white text-slate-600 ring-1 ring-slate-200 hover:bg-slate-50")}>{item}</button>)}</div><div className="mt-9 flex items-end justify-between"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#4a8558]">Current catalogue</p><h2 className="mt-2 text-3xl font-black tracking-[-.04em]">{visible.length} stays to compare</h2></div><p className="hidden items-center gap-1.5 text-sm text-slate-500 sm:flex"><BedDouble size={16} /> Select rooms on the stay page</p></div>{visible.length ? <div className="mt-7 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{visible.map((stay) => <article key={stay.id} className="overflow-hidden rounded-[1.6rem] bg-white shadow-sm ring-1 ring-slate-200"><img src={stay.image} alt={stay.name} className="h-56 w-full object-cover" /><div className="p-5"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-wider text-[#4a8558]">{stay.type}</p><h3 className="mt-1 text-xl font-black">{stay.name}</h3></div><span className="flex items-center gap-1 text-sm font-black text-amber-700"><Star size={14} className="fill-amber-400 text-amber-400" /> {stay.rating}</span></div><p className="mt-2 flex items-center gap-1.5 text-sm text-slate-500"><MapPin size={15} /> {stay.destination}, {stay.state}</p><div className="mt-4 flex flex-wrap gap-2">{stay.amenities.slice(0, 2).map((amenity) => <span key={amenity} className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-600">{amenity}</span>)}</div><div className="mt-5 flex items-center justify-between border-t border-slate-100 pt-4"><p><b className="text-lg">{formatINR(stay.price)}</b><span className="text-xs text-slate-500"> / night</span></p><Link to={"/hotels/" + stay.id} className="inline-flex items-center gap-1.5 text-sm font-bold text-[#245239]">View stay <ArrowRight size={16} /></Link></div></div></article>)}</div> : <div className="mt-8 rounded-3xl bg-white px-6 py-16 text-center ring-1 ring-slate-200"><Search size={26} className="mx-auto text-slate-400" /><h3 className="mt-4 text-xl font-black">No stays match that search.</h3></div>}</section></main>;
}
