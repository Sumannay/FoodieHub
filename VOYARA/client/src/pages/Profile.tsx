import { CalendarDays, ChevronRight, Heart, LogOut, MapPin, ReceiptText, Settings, Star } from "lucide-react";
import { Link } from "react-router-dom";
import { useVoyara } from "../context/VoyaraContext";
import { formatINR } from "../data/catalog";

export default function Profile() {
  const { bookings, favorites, signOut, user } = useVoyara();
  if (!user) return null;
  const initials = user.name.split(" ").map((part) => part[0]).join("").slice(0, 2).toUpperCase();
  const accountLinks = [{ label: "Saved places", detail: `${favorites.length} items`, icon: Heart, to: "/favorites" }, { label: "My bookings", detail: `${bookings.length} bookings`, icon: ReceiptText, to: "/trips" }, { label: "Account settings", detail: "Preferences and privacy", icon: Settings, to: "/settings" }];
  return <main className="min-h-[70vh] bg-[#fbfaf7] px-5 py-10 sm:px-8 lg:px-10"><div className="mx-auto max-w-6xl"><section className="overflow-hidden rounded-[1.8rem] bg-[#183927] p-6 text-white sm:p-9"><div className="flex flex-col justify-between gap-7 sm:flex-row sm:items-center"><div className="flex items-center gap-5"><span className="grid size-16 place-items-center rounded-3xl bg-[#d8fbcc] text-xl font-black text-[#183927]">{initials}</span><div><p className="text-sm font-bold text-[#d8fbcc]">{user.role.toLowerCase().replace("_", " ")}</p><h1 className="mt-1 text-3xl font-black tracking-tight">{user.name}</h1><p className="mt-1 text-white/60">{user.email}</p></div></div><button type="button" onClick={signOut} className="inline-flex items-center justify-center gap-2 rounded-full border border-white/20 px-4 py-2.5 text-sm font-bold text-white/85 hover:bg-white/10"><LogOut size={16} /> Sign out</button></div><div className="mt-9 grid max-w-xl grid-cols-3 gap-4 border-t border-white/10 pt-6"><div><p className="text-2xl font-black">{bookings.length}</p><p className="mt-1 text-xs font-bold uppercase tracking-wider text-white/50">Bookings</p></div><div><p className="text-2xl font-black">{favorites.length}</p><p className="mt-1 text-xs font-bold uppercase tracking-wider text-white/50">Saved</p></div><div><p className="text-2xl font-black">0</p><p className="mt-1 text-xs font-bold uppercase tracking-wider text-white/50">Reviews</p></div></div></section><div className="mt-9 grid gap-8 lg:grid-cols-[1.12fr_.88fr]"><section><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#4a8558]">Your plans</p><h2 className="mt-2 text-2xl font-black">Upcoming trips</h2></div><Link to="/trips/create" className="text-sm font-bold text-[#245239] hover:underline">Plan a trip</Link></div>{bookings.length === 0 ? <div className="mt-5 rounded-3xl bg-white px-6 py-12 text-center ring-1 ring-slate-200"><span className="mx-auto grid size-12 place-items-center rounded-2xl bg-[#eef4e9] text-[#387a43]"><CalendarDays size={21} /></span><h3 className="mt-4 text-lg font-black">Your next trip starts here.</h3><p className="mt-2 text-sm text-slate-500">Choose a place you love, then let us help make the details easy.</p><Link to="/explore" className="mt-5 inline-flex rounded-full bg-[#183927] px-4 py-2.5 text-sm font-bold text-white">Explore destinations</Link></div> : <div className="mt-5 space-y-3">{bookings.map((booking) => <article key={booking.id} className="flex flex-col gap-4 rounded-3xl bg-white p-5 shadow-sm ring-1 ring-slate-200 sm:flex-row sm:items-center sm:justify-between"><div className="flex gap-4"><span className="grid size-11 shrink-0 place-items-center rounded-2xl bg-[#eef4e9] text-[#387a43]"><MapPin size={19} /></span><div><p className="font-black">{booking.destination}</p><p className="mt-1 text-sm text-slate-500">{booking.stay} · {booking.checkIn || "Dates to be confirmed"}</p><p className="mt-2 text-xs font-bold text-[#387a43]">{booking.id} · {booking.status}</p></div></div><p className="text-lg font-black">{formatINR(booking.total)}</p></article>)}</div>}</section><aside><p className="text-xs font-bold uppercase tracking-[.18em] text-[#4a8558]">Account</p><div className="mt-4 divide-y divide-slate-100 overflow-hidden rounded-3xl bg-white ring-1 ring-slate-200">{accountLinks.map(({ label, detail, icon: Icon, to }) => <Link key={label} to={to} className="flex items-center gap-4 p-5 transition hover:bg-slate-50"><span className="grid size-10 place-items-center rounded-xl bg-slate-100 text-slate-600"><Icon size={18} /></span><span className="min-w-0 flex-1"><b className="block text-sm">{label}</b><span className="mt-0.5 block text-xs text-slate-500">{detail}</span></span><ChevronRight size={18} className="text-slate-300" /></Link>)}</div><div className="mt-6 rounded-3xl bg-[#eef4e9] p-6"><Star size={19} className="fill-amber-400 text-amber-400" /><h3 className="mt-3 font-black">Share what you loved.</h3><p className="mt-2 text-sm leading-6 text-slate-600">After a completed trip, rate the stays and places that made it special.</p><Link to="/reviews" className="mt-4 inline-block text-sm font-bold text-[#245239] hover:underline">View reviews</Link></div></aside></div></div></main>;
}
